/**
 * Класс логгера
 * Добавляем в уже существующий проект метод логгирования
 * Используем декоратор
 */
public class Logger {
    public void log(String line) {
        System.out.println(line);
    }
}
